# Playwright browser cache.
# Leave unset for the default: ~/.cache/ms-playwright
#
# Cursor Agent may inject PLAYWRIGHT_BROWSERS_PATH pointing at a sandbox
# cache. On your own terminal, unset it (or leave blank) so tests use
# the browsers installed by: npm run test:setup
#
# PLAYWRIGHT_BROWSERS_PATH=
